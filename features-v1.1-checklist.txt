ARC — Features v1.2 MVP Checklist
===================================

21 total features. 14 built. 7 remaining.
Claude Code: verify each feature against the codebase. Mark PASS, PARTIAL, or MISSING.

===================================
BUILT FEATURES (14) — Verify these work
===================================

F1: ARC WORDMARK
  - Gold #C9A84C wordmark top-left of Today screen
  - Instrument Sans, small caps (fontVariant: 'small-caps'), letter-spacing 0.2em
  - File: TodayScreen.tsx
  - Status: [ ]

F2: DATE HEADER + NAVIGATION
  - Formatted date display (e.g. "TUESDAY, MAR 3")
  - Left/right chevron arrows navigate past days
  - Right arrow hidden when on today
  - Past days show read-only indicator
  - Cannot navigate to future dates
  - File: components/DateHeader.tsx
  - Status: [ ]

F4: DAILY INTENT
  - Single-line text input
  - Placeholder: "What is today's intent?"
  - Bottom border only styling
  - Disabled on past days
  - Persists to localStorage on every keystroke
  - File: components/DailyIntent.tsx
  - Status: [ ]

F5: DOMAIN CARDS (BODY / BUILD / EXPOSURE)
  - Three cards mapped from DOMAINS config array
  - Surface background #1A1A1A, rounded corners, 16px padding
  - 3px gold left border when complete
  - Domain label: uppercase, secondary text color
  - Task text input with domain-specific placeholder
  - Labels: BODY / BUILD / EXPOSURE (display), body / build / exposure (keys)
  - File: components/DomainCard.tsx, config/domains.ts, types/index.ts
  - Status: [ ]

F6: SWIPE-TO-COMPLETE
  - useSwipe hook detects horizontal drag
  - 10px dead zone before gesture activates
  - 40% of card width threshold to trigger
  - Card follows finger/mouse during drag (translateX)
  - On complete: gold tint bg (rgba 201,168,76,0.15), gold left border, gold checkmark, gold label
  - 200ms ease snap animation
  - Vertical scroll not hijacked (cancel if vertical > horizontal)
  - Task input still tappable without triggering swipe
  - Swipe only marks DONE on incomplete cards (not undo)
  - File: hooks/useSwipe.ts, components/DomainCard.tsx
  - Status: [ ]

F9: READ-ONLY PAST DAYS
  - All inputs disabled on past days (intent + 3 tasks + 3 swipes)
  - Opacity 0.5 on past day cards
  - pointerEvents: 'none' on disabled cards
  - No retroactive check-offs after midnight
  - File: screens/TodayScreen.tsx, components/DomainCard.tsx
  - Status: [ ]

F10: MIDNIGHT ROLLOVER
  - useDateRollover hook with dual detection
  - 60-second interval check (only when document.visibilityState === 'visible')
  - visibilitychange listener triggers check when tab becomes visible
  - When date flips: selectedDate snaps to new today
  - Previous day becomes permanently read-only
  - File: hooks/useDateRollover.ts
  - Status: [ ]

F12: VELOCITY STATUS LABEL
  - Large centered text, 28px bold, Instrument Sans
  - LOCKED IN = #C9A84C (gold) — avg 2.5-3.0
  - MOVING = #F5F5F0 (white) — avg 1.5-2.4
  - DRAG DETECTED = #888888 (gray) — avg 0.5-1.4
  - FLATLINE = #333333 (dark gray) — avg 0-0.4
  - Weekly average displayed below as "X.X avg"
  - File: components/StatusLabel.tsx
  - Status: [ ]

F14: WEEKLY VELOCITY CHART
  - Recharts BarChart with ResponsiveContainer
  - Per-bar coloring via Cell components
  - X-axis: day labels in #888 Instrument Sans
  - No Y-axis, no grid, no tooltip, no legend
  - ~200px height
  - File: components/VelocityChart.tsx
  - Status: [ ]

F17: PER-DOMAIN BREAKDOWN
  - Three horizontal rows, one per domain from DOMAINS config
  - Domain label left, count right
  - Rounded progress bar with gold fill proportional to count
  - Background #1A1A1A
  - SPEC NOTE: denominator should be non-ghost days, not hardcoded 7
  - File: components/DomainBreakdown.tsx
  - Status: [ ]

F18: WEEKLY AVERAGE DISPLAY
  - Numeric average below status label
  - Secondary text color #888888
  - Formatted to one decimal place
  - File: components/StatusLabel.tsx
  - Status: [ ]

F19: LOCALSTORAGE PERSISTENCE
  - loadDay(dateStr): reads and parses localStorage key
  - saveDay(data): writes stringified DayData
  - getDateKey(date): formats Date to "YYYY-MM-DD"
  - Key pattern: arc-YYYY-MM-DD
  - Try-catch wrapped for corrupted data resilience
  - Immediate write on every state change
  - CRITICAL: loadDay MUST return null for missing keys (not default empty)
  - CRITICAL: dayExists(dateStr) function must exist
  - CRITICAL: getMonday(date) function must exist
  - File: utils/storage.ts
  - Status: [ ]

F20: BOTTOM NAVIGATION
  - Fixed bottom bar, 56px height
  - Two tabs: Today and Velocity
  - Active tab: #C9A84C gold
  - Inactive tab: #888888 gray
  - Minimum 44px tap targets
  - Safe area padding for notched phones (env safe-area-inset-bottom)
  - File: components/BottomNav.tsx
  - Status: [ ]

F21: PWA
  - manifest.json: name "Arc", theme_color #0A0A0A, display standalone
  - Icons: 192x192 and 512x512
  - Service worker with cache-first strategy
  - Meta tags: theme-color, apple-mobile-web-app-capable
  - Registered in main.tsx
  - File: public/manifest.json, public/sw.js, src/main.tsx
  - Status: [ ]

===================================
NEW FEATURES (7) — Verify implemented
===================================

F3: 3-DAY MOMENTUM BARS
  - Three small vertical bars, top-right of Today screen
  - Shows last 3 days (not including today)
  - Each bar: ~6px wide, ~32px tall, 4px gap between
  - 3/3 done = full gold fill
  - 2/3 done = two-thirds gold fill from bottom
  - 1/3 done = one-third gold fill from bottom
  - 0/3 done but key exists (present) = dim gold outline (#C9A84C at 30%)
  - No key (ghost day) = dark gray #333333, barely visible
  - File: components/MomentumBars.tsx
  - Status: [ ]

F7: HOLD-TO-UNDO
  - useLongPress hook exists
  - 500ms press-and-hold on completed card triggers undo
  - Timer starts on touchstart/mousedown
  - Cancels if finger lifts before 500ms
  - Cancels if finger/mouse moves more than 10px
  - Optional: navigator.vibrate(50) if available
  - Only active on completed cards (no-op on incomplete)
  - Disabled on past days
  - Must REPLACE swipe-to-undo (swipe should NOT undo anymore)
  - When undone: gold fades out, task text becomes editable again
  - File: hooks/useLongPress.ts, components/DomainCard.tsx
  - Status: [ ]

F8: DRIFT SIGNAL
  - For each domain, check last 3 days (not including today)
  - If domain.done === false (or ghost day) for all 3: domain is drifting
  - Drifting card renders at ~60% opacity on ALL elements (label, input, border)
  - No notification, no warning text, no badge
  - Clears immediately when domain is completed today
  - Needs 3 consecutive missed days to trigger
  - Fewer than 3 prior days = no drift yet
  - File: screens/TodayScreen.tsx (computes drift), components/DomainCard.tsx (drifting prop)
  - Status: [ ]

F11: GHOST DAY DETECTION
  - loadDay returns NULL for missing keys (not default empty DayData)
  - dayExists(dateStr) returns boolean (key exists in localStorage)
  - Ghost day = no localStorage key exists = never opened app
  - Present day = key exists, all done: false
  - Active day = key exists, 1+ done: true
  - TodayScreen only creates/saves DayData on FIRST user interaction
  - Opening app and doing nothing = stays ghost (no key created)
  - File: utils/storage.ts, screens/TodayScreen.tsx
  - Status: [ ]

F13: EARLY-WEEK STATUS DIMMING
  - Monday through Wednesday (dayOfWeek 1-3):
    Status text renders in #888888 regardless of actual tier
  - Thursday through Sunday (dayOfWeek 4-7):
    Status renders in full designated color per tier
  - WeeklyData type must include dayOfWeek: number field
  - No tooltip or explanation
  - File: components/StatusLabel.tsx, utils/velocity.ts, types/index.ts
  - Status: [ ]

F15: MON-SUN FIXED WEEK
  - Velocity chart shows current Monday through Sunday (NOT rolling 7 days)
  - getMonday(date) utility returns Monday of the week containing date
  - Only include days up to and including today (future days excluded)
  - Future days = empty space in chart (not rendered)
  - Ghost days (null from loadDay) EXCLUDED from average calculation
  - Average denominator = count of non-ghost days only
  - If all days are ghost, average = 0
  - File: utils/velocity.ts, utils/storage.ts (getMonday), components/VelocityChart.tsx
  - Status: [ ]

F16: PROPORTIONAL THIRD-FILL BARS
  - Bars fill in thirds from bottom (not solid blocks)
  - 1/3 done = bottom third gold, top two-thirds empty
  - 2/3 done = bottom two-thirds gold, top third empty
  - 3/3 done = full gold
  - Ghost day = gap (no bar at all)
  - Present-but-incomplete (0/3, key exists) = dim gold outline (#C9A84C at 30%)
  - Same visual language in BOTH MomentumBars AND VelocityChart
  - File: components/VelocityChart.tsx, components/MomentumBars.tsx
  - Status: [ ]

===================================
EDGE CASES TO VERIFY
===================================

EC1: GHOST WEEK
  - When ALL days Mon-Sun have zero localStorage keys
  - VelocityScreen must show: empty chart, NO bars, NO outlines
  - NO status label rendered (not even FLATLINE) — just absence
  - Visually distinct from a bad week (which has bars)
  - Must not crash
  - Status: [ ]

EC2: DOMAIN LABEL CONSISTENCY (RESOLVED)
  - Labels: BODY / BUILD / EXPOSURE (display), body / build / exposure (keys)
  - This is settled. Just confirm no stale "physical" / "output" / "social" strings anywhere.
  - Status: [ ]

EC3: SAFE AREA / IPHONE NOTCH
  - Top padding must use env(safe-area-inset-top) + extra 16px
  - Bottom nav must use env(safe-area-inset-bottom)
  - Content must not be obscured by notch or home indicator
  - Status: [ ]

EC4: NO DEBUG CODE
  - No console.log statements left in production code
  - No TODO comments that indicate unfinished work
  - No commented-out code blocks
  - Status: [ ]

===================================
POST-SPEC ADDITIONS (verify if present)
===================================

PS1: GITHUB ICON LINK
  - Small GitHub octocat SVG icon, centered, above bottom nav
  - 20px size, fill #888888
  - Links to https://github.com/anishpavaskar/arc
  - Opens in new tab
  - Status: [ ]

PS2: INTENT LOCK-IN (v1.3 planned, NOT YET REQUIRED)
  - Long-press intent line to seal the day's commitment
  - committed: boolean field in DayData
  - Gold lock icon, task inputs become non-editable
  - NOT YET IN SPEC — just note if present or absent
  - Status: [ ]

===================================
BUILD VERIFICATION
===================================

- [ ] npm run build — zero errors
- [ ] npm run build — zero warnings
- [ ] npm test — all tests pass
- [ ] npx tsc --noEmit — zero type errors

===================================
FUTURE EARNED FEATURES (not in scope)
===================================

- Task weighting (MAINTAIN/ADVANCE/EXPAND) — 3 weeks daily use
- Weekly reflection prompt (text field on velocity) — 3 weeks daily use
- Quarterly goal view — 3 consecutive weekly reviews
- Eliminate goal tracking — 2 weeks daily use
- Push notifications — 4 weeks daily use
- Data export (CSV/JSON) — any time
- Peer accountability pod — 8 weeks + 3 people
- Native mobile app — 12 weeks consistent usage